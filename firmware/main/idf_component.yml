dependencies:
  joltwallet/littlefs: "^1.14.0"
  espressif/mdns: "^1.3.0"
  espressif/mqtt:
    version: "^1.1.0"
    rules:
      - if: "idf_version >= 6.0"
  espressif/cjson:
    version: "^1.7.0"
    rules:
      - if: "idf_version >= 6.0"

